fix: connector label editor overlays label exactly; transparent-then-opaque background

- ConnectorLabelEditor now mirrors Konva label dimensions (chars*8+8 width, 20h) scaled by zoom
- Editor is centered on the connector midpoint in screen space
- Konva label hides while editing so the HTML input overlays it seamlessly
- Label background changed to fully opaque white with a thin connector-colored border
- Label font and sizing scale with zoom level
